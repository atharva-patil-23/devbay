import mongoose from "mongoose"


const connectDB = () => {
    try {
        mongo

        console.log(first)
    } catch (error) {
        console.log('mongoDB connection error',error)
        process.exit(1)
    }
}